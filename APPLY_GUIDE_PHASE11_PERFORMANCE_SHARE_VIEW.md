# Phase11 / 4차 적용 안내

기준: 실제 작업 폴더에 존재하던 최신 산출물 `worship-db-phase9-memo-foundation.zip` 기반에 4차 연주화면/공유화면 표시 개선만 적용했습니다.

## 수정 파일
- `mobile.html`
- `share.html`

## 적용 내용
1. 연주화면 프레임/박스 느낌 제거
   - 악보가 화면 전체를 차지하도록 유지
   - 별도 카드 테두리나 틀 스타일을 오버라이드

2. 상단 투명 오버레이 정리
   - 왼쪽: `1페이지 보기`, `2페이지 보기`
   - 오른쪽: `X`
   - 악보 위에 얹히는 투명 레이어 방식

3. 하단 콘티 흐름 표시
   - 콘티 곡 제목을 하단 가운데 정렬
   - 현재곡: 노랑
   - 같은 곡명: 흰색
   - 이전/다음 곡: 빨강
   - 곡 제목을 누르면 해당 곡으로 이동

4. 메모 표시
   - 현재 콘티 곡에 `memo`, `conti_memo`, `note` 중 값이 있으면 하단 곡 목록 위에 한 줄 표시
   - 표시만 추가했고, 저장 구조는 건드리지 않았습니다.

5. 화면잠금
   - 하단 오른쪽 고정
   - 기존 Wake Lock 동작 유지

6. 공유 연주화면에도 동일한 규칙 적용
   - 공유 페이지에서 연주화면 진입 시 동일한 투명 오버레이/색상 규칙 사용

## 건드리지 않은 것
- Supabase 저장 구조
- 곡/콘티 데이터 구조
- 메모 저장/불러오기 구조
- 버전 관리 구조
- 기존 악보 로딩 함수
- 기존 공유 링크 생성 함수

## 주의
- 같은 곡 판정은 현재 데이터에서 사용할 수 있는 `곡명` 기준으로 처리했습니다.
- 추후 버전/페이지 그룹 ID가 명확해지면 같은 곡 판정을 `songId + versionId/pageGroup` 기준으로 더 정확하게 바꾸는 것이 좋습니다.
