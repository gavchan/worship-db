# Phase12-3 콘티 리스트 / 오른쪽 상세 수정·업로드 패치

기준:
- 기존 화면 구조 최대 유지
- 왼쪽 콘티 순서 리스트는 탐색용으로만 단순화
- 수정/악보 업로드는 오른쪽 선택곡 상세 패널과 수정 모달 안에서 처리

수정 파일:
- mobile.html

변경 내용:
1. 왼쪽 콘티 순서 리스트
   - 표시 항목을 `순서 / 곡이름 / Key / 속도 / 악보 / 영상`으로 정리
   - `주성`, `수정` 컬럼 제거
   - 각 곡 아래로 수정 영역이 떨어지지 않도록 정리

2. 오른쪽 곡별 보기 패널
   - 선택곡 상세 영역에 `수정`, `악보 업로드` 버튼 추가
   - 두 버튼 모두 현재 선택된 곡의 수정 모달을 열도록 연결

3. 수정 모달
   - 기존 Key/BPM/영상/메모 수정 유지
   - `현재 곡 악보 업로드` 섹션 추가
   - JPG/PNG 다중 업로드 지원
   - 기존 곡별 악보 목록 표시
   - 악보 보기 / 삭제 지원

주의:
- 악보 업로드는 콘티 임시 설정이 아니라 곡별 악보 저장소에 실제 업로드됩니다.
- DB에 연결되지 않은 직접 입력 곡은 악보 업로드가 제한됩니다.
